server:
  port: 8888

server-resource:
  url-prefix: /attachment/
  attachment-path-prefix: D:/Attachment/

spring:
  jpa:
    database: postgresql
    hibernate.ddl-auto: update
    show-sql: false
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://
    username:
    password: