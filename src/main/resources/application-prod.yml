server:
  port: 8888

server-resource:
  url-prefix: /attachment/
  attachment-path-prefix: /root/server-deploy/approval-server/attachment/

spring:

  jpa:
    database: mysql
    hibernate.ddl-auto: update
    show-sql: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://
    username:
    password:

